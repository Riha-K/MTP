# BareSoil S1 pipeline dependencies (install inside LULCDial-s1 venv)
rasterio>=1.3.0
datasets>=2.14.0
tqdm>=4.65.0
numpy>=1.24.0
Pillow>=10.0.0
tifffile>=2023.0.0
# CNN baseline (on PARAM use module MLDL/Pytorch-gpu instead of pip torch)
torch>=2.0.0
torchvision>=0.15.0
